[build-system]
requires = ["maturin>=1.8,<2"]
build-backend = "maturin"

[project]
name = "sunpack-native"
dynamic = ["version"]
description = "Native byte scanning helpers for SunPack."
requires-python = ">=3.10"

[tool.maturin]
module-name = "sunpack_native"
bindings = "pyo3"
